🔹 What is Padding?

The Padding widget in Flutter is used to create empty space around another widget (inside the parent).

It helps in managing spacing and layout.



🔹 Why Use Padding?

To prevent text or widgets from touching the edges.

To improve UI readability and design.

To add spacing without using multiple SizedBox widgets.



🔹 How it Works

padding: EdgeInsets.all(30) → adds 30 pixels space on all sides (top, bottom, left, right).

The child widget (like Text) sits inside the padding.



🔹 Key Properties

padding → Defines the space around the child.

EdgeInsets.all(value) → same space on all sides.

EdgeInsets.symmetric(horizontal, vertical) → separate horizontal & vertical padding.

EdgeInsets.only(left, top, right, bottom) → padding on specific sides.

child → The widget you want to apply padding to (e.g., Text, Container, Row, etc.).



🔹 When to Use

When you want spacing inside layouts without shifting other widgets.

Example: Adding space around a button, text, or image.